pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,ts,vue}"
      run: pnpm lint:fix
    format:
      glob: "*.{js,ts,vue,json,md}"
      run: pnpm format
    type-check:
      run: pnpm type-check

pre-push:
  commands:
    test:
      run: pnpm test
